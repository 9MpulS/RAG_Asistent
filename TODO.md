# TODO: Налаштування та запуск RAG-асистента

## 📋 Чеклист для запуску проекту

### 1. ⚙️ Налаштування середовища

- [ ] **Перевірити версію Python**
  ```bash
  python --version  # Має бути >= 3.10
  ```

- [ ] **Активувати віртуальне середовище**
  ```bash
  # Windows
  .venv\Scripts\activate

  # Linux/Mac
  source .venv/bin/activate
  ```

- [ ] **Встановити всі залежності**
  ```bash
  uv pip install -r requirements.txt
  ```

  Або встановити вручну:
  ```bash
  uv pip install fastapi uvicorn sqlalchemy psycopg2-binary pgvector pydantic-settings pydantic-ai openai cohere tiktoken crawl4ai pypdf python-multipart beautifulsoup4
  ```

### 2. 🗄️ Налаштування бази даних

- [ ] **Встановити PostgreSQL 15+**
  - Завантажити з: https://www.postgresql.org/download/
  - Або через Docker:
    ```bash
    docker run -d --name postgres-rag -e POSTGRES_PASSWORD=password -e POSTGRES_DB=rag_assistant -p 5432:5432 postgres:15
    ```

- [ ] **Встановити розширення pgvector**

  **Варіант 1 - Якщо PostgreSQL встановлений локально:**
  ```bash
  # Для Ubuntu/Debian
  sudo apt install postgresql-15-pgvector

  # Для macOS (через Homebrew)
  brew install pgvector

  # Для Windows - завантажити з GitHub:
  # https://github.com/pgvector/pgvector/releases
  ```

  **Варіант 2 - Docker з pgvector:**
  ```bash
  docker run -d --name postgres-rag \
    -e POSTGRES_PASSWORD=password \
    -e POSTGRES_DB=rag_assistant \
    -p 5432:5432 \
    ankane/pgvector
  ```

- [ ] **Перевірити підключення до БД**
  ```bash
  # Через psql
  psql -h localhost -U postgres -d rag_assistant

  # Або через pgAdmin
  ```

### 3. 🔑 Отримання API ключів

- [ ] **Зареєструватись та отримати Grok API key**
  1. Перейти на https://x.ai/api
  2. Зареєструватись/увійти
  3. Створити API key
  4. Зберегти ключ у безпечному місці

- [ ] **Зареєструватись та отримати Cohere API key**
  1. Перейти на https://dashboard.cohere.com/
  2. Зареєструватись (можна через Google/GitHub)
  3. Перейти в розділ "API Keys"
  4. Створити новий ключ (Trial key - безкоштовний)
  5. Зберегти ключ

### 4. 📝 Налаштування .env файлу

- [ ] **Створити .env файл**
  ```bash
  cp .env.example .env
  ```

- [ ] **Заповнити .env файл своїми даними**

  Відкрити `.env` та вказати:

  ```bash
  # Database
  DATABASE_URL=postgresql://postgres:password@localhost:5432/rag_assistant

  # Grok API (xAI)
  GROK_API_KEY=xai-ваш_ключ_тут
  GROK_MODEL=grok-beta

  # Cohere API (для embeddings)
  COHERE_API_KEY=ваш_cohere_ключ_тут
  EMBEDDING_MODEL=embed-multilingual-v3.0
  VECTOR_DIMENSIONS=1024

  # RAG Settings (можна залишити за замовчуванням)
  CHUNK_SIZE=500
  CHUNK_OVERLAP=50
  TOP_K=5
  SIMILARITY_THRESHOLD=0.7
  ```

### 5. 🗃️ Ініціалізація бази даних

- [ ] **Запустити скрипт ініціалізації БД**
  ```bash
  python scripts/init_db.py
  ```

  Очікуваний результат:
  ```
  ✓ Database initialized
  Created tables:
    - documents
    - chunks
  Enabled extensions:
    - pgvector
  ```

- [ ] **Перевірити створення таблиць**
  ```sql
  -- Підключитись до БД та виконати:
  \dt  -- показати всі таблиці

  -- Має бути:
  -- documents
  -- chunks
  ```

### 6. 🚀 Запуск додатку

- [ ] **Запустити FastAPI сервер**
  ```bash
  # Варіант 1 (з auto-reload)
  uvicorn main:app --reload

  # Варіант 2 (через main.py)
  python main.py

  # Варіант 3 (на конкретному порті)
  uvicorn main:app --reload --host 0.0.0.0 --port 8000
  ```

- [ ] **Перевірити що сервер запустився**

  Відкрити в браузері:
  - http://localhost:8000 - головна сторінка
  - http://localhost:8000/docs - Swagger UI (документація API)
  - http://localhost:8000/health - health check

### 7. 📄 Завантаження тестового документа

- [ ] **Варіант 1: Завантажити через API (файл)**

  Підготувати PDF/TXT файл з нормативним документом

  ```bash
  curl -X POST http://localhost:8000/api/documents \
    -F "file=@document.pdf" \
    -F "title=Положення про стипендіальне забезпечення" \
    -F "document_number=2133"
  ```

- [ ] **Варіант 2: Завантажити через API (URL)**

  ```bash
  curl -X POST http://localhost:8000/api/documents \
    -H "Content-Type: application/json" \
    -d '{
      "url": "https://normative.sumdu.edu.ua/index.php?task=getfile&tmpl=component&id=XXXX",
      "title": "Назва документа"
    }'
  ```

- [ ] **Варіант 3: Тестовий скрипт парсингу**

  ```bash
  python scripts/crawl_sumdu.py --url "https://normative.sumdu.edu.ua/..."
  ```

- [ ] **Перевірити статус обробки документа**
  ```bash
  curl http://localhost:8000/api/documents
  ```

  Статус має змінитись з `processing` → `completed`

### 8. 🧪 Тестування RAG запитів

- [ ] **Виконати тестовий запит**

  ```bash
  curl -X POST http://localhost:8000/api/query \
    -H "Content-Type: application/json" \
    -d '{
      "query": "Які умови отримання стипендії?",
      "top_k": 5
    }'
  ```

- [ ] **Перевірити відповідь**

  Має містити:
  - `answer` - згенерована відповідь
  - `sources` - список джерел з посиланнями
  - `reasoning_path` - шлях міркувань SGR

- [ ] **Протестувати через Swagger UI**

  Відкрити http://localhost:8000/docs та виконати запит через інтерфейс

### 9. ✅ Перевірка всієї системи

- [ ] **Health check детальний**
  ```bash
  curl http://localhost:8000/health/detailed
  ```

  Має показати:
  - Статус БД
  - Статус Grok API
  - Кількість документів
  - Кількість chunks

- [ ] **Перевірити всі CRUD операції**
  - Створення документа ✓
  - Отримання списку документів ✓
  - Отримання деталей документа ✓
  - Оновлення документа ✓
  - Видалення документа (опціонально)

- [ ] **Перевірити RAG pipeline**
  - Запит обробляється без помилок ✓
  - Відповідь релевантна запиту ✓
  - Джерела правильно відформатовані ✓

---

## 🐛 Troubleshooting (якщо щось не працює)

### Проблема: PostgreSQL не підключається

**Рішення:**
```bash
# Перевірити чи запущений PostgreSQL
sudo systemctl status postgresql  # Linux
brew services list  # Mac
# або через Services.msc на Windows

# Перевірити DATABASE_URL в .env
# Формат: postgresql://user:password@host:port/database
```

### Проблема: pgvector не встановлено

**Рішення:**
```sql
-- Підключитись до БД та виконати:
CREATE EXTENSION IF NOT EXISTS vector;

-- Перевірити:
SELECT * FROM pg_extension WHERE extname = 'vector';
```

### Проблема: Помилка з API ключами

**Рішення:**
- Перевірити що ключі правильно вказані в `.env`
- Перевірити що немає зайвих пробілів/лапок
- Перезапустити сервер після зміни `.env`

### Проблема: ImportError з пакетами

**Рішення:**
```bash
# Перевстановити всі залежності
uv pip install --upgrade -r requirements.txt

# Або конкретний пакет
uv pip install --upgrade cohere
```

### Проблема: Crawl4AI не працює

**Рішення:**
- Перевірити інтернет з'єднання
- Спробувати інший URL
- Перевірити логи сервера

### Проблема: Документ не обробляється

**Перевірити:**
```bash
# Подивитись статус документа
curl http://localhost:8000/api/documents/{id}

# Перевірити логи сервера
# Має бути інформація про помилку
```

---

## 📚 Корисні команди

### Управління БД
```bash
# Скинути БД (видалить всі дані!)
python scripts/init_db.py --drop

# Переініціалізувати
python scripts/init_db.py
```

### Перевірка стану
```bash
# Health check
curl http://localhost:8000/health

# Детальний статус
curl http://localhost:8000/health/detailed

# Список документів
curl http://localhost:8000/api/documents
```

### Робота з документами
```bash
# Отримати деталі документа
curl http://localhost:8000/api/documents/1

# Оновити документ
curl -X PUT http://localhost:8000/api/documents/1 \
  -H "Content-Type: application/json" \
  -d '{"title": "Нова назва"}'

# Видалити документ
curl -X DELETE http://localhost:8000/api/documents/1

# Перегенерувати chunks
curl -X POST http://localhost:8000/api/documents/1/reprocess
```

---

## 🎯 Що далі?

Після успішного запуску ви можете:

1. **Завантажити більше документів** з normative.sumdu.edu.ua
2. **Налаштувати параметри RAG** в `.env` (CHUNK_SIZE, TOP_K, тощо)
3. **Тестувати різні запити** для оцінки якості відповідей
4. **Інтегрувати з фронтендом** або Telegram ботом
5. **Налаштувати production deployment** (Docker, Railway, тощо)

---

## 📞 Підтримка

Якщо виникають проблеми:
1. Перевірити логи сервера
2. Подивитись документацію в `TZ_ARCHITECTURE.txt`
3. Перевірити API документацію на `/docs`
4. Переглянути приклади в `README.md`

**Успіхів! 🚀**
